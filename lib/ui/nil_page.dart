import 'package:flutter/material.dart';

class NasaImageLibraryPage extends StatelessWidget {
  const NasaImageLibraryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Nasa image library"));
  }
}
